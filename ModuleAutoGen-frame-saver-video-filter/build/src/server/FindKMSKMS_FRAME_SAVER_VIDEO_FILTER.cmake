# - Try to find KMSKMS_FRAME_SAVER_VIDEO_FILTER library

#=============================================================================
# Copyright 2014 Kurento
#
#=============================================================================

set(PACKAGE_VERSION "0.0.1~0.g69409c5")
set(KMSKMS_FRAME_SAVER_VIDEO_FILTER_VERSION ${PACKAGE_VERSION})

message (STATUS "Looking for KMSKMS_FRAME_SAVER_VIDEO_FILTER: 0.0.1~0.g69409c5")

include (GenericFind)

generic_find (
  REQUIRED
  LIBNAME KMSCORE
  VERSION ^6.0.0
)

generic_find (
  REQUIRED
  LIBNAME KMSELEMENTS
  VERSION ^6.0.0
)

generic_find (
  REQUIRED
  LIBNAME KMSFILTERS
  VERSION ^6.0.0
)

set (REQUIRED_VARS
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_VERSION
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARY
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARIES
)

set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_BINARY_DIR_PREFIX "build" CACHE PATH "Path prefix used to look for binary files")
set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_SOURCE_DIR_PREFIX "" CACHE PATH "Path prefix used to look for source files")

set(KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS
  ${KMSCORE_INCLUDE_DIRS}
  ${KMSELEMENTS_INCLUDE_DIRS}
  ${KMSFILTERS_INCLUDE_DIRS}
)

if (NOT "FrameSaverVideoFilter.hpp FrameSaverVideoFilterInternal.hpp" STREQUAL " ")
  if (TARGET kmskms_frame_saver_video_filterinterface)
    set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_INCLUDE_DIR "${KMSKMS_FRAME_SAVER_VIDEO_FILTER_BINARY_DIR_PREFIX}/src/server/interface/generated-cpp")
  else ()
    find_path(KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_INCLUDE_DIR
      NAMES
        FrameSaverVideoFilter.hpp
        FrameSaverVideoFilterInternal.hpp
      PATH_SUFFIXES
        ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_BINARY_DIR_PREFIX}/src/server/interface/generated-cpp
        kurento/modules/kms_frame_saver_video_filter
    )
  endif ()

  list (APPEND KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_INCLUDE_DIR})
  list (APPEND REQUIRED_VARS KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_INCLUDE_DIR)
endif ()

if (NOT "FrameSaverVideoFilterImplFactory.hpp" STREQUAL "")
  if (TARGET kmskms_frame_saver_video_filterimpl)
    set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_INTERNAL_INCLUDE_DIR "${KMSKMS_FRAME_SAVER_VIDEO_FILTER_BINARY_DIR_PREFIX}/src/server/implementation/generated-cpp")
  else ()
    find_path(KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_INTERNAL_INCLUDE_DIR
      NAMES
        FrameSaverVideoFilterImplFactory.hpp
      PATH_SUFFIXES
        ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_BINARY_DIR_PREFIX}/src/server/implementation/generated-cpp
        kurento/modules/kms_frame_saver_video_filter
    )
  endif ()

  list (APPEND KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_INTERNAL_INCLUDE_DIR})
  list (APPEND REQUIRED_VARS KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_INTERNAL_INCLUDE_DIR)
endif ()

if (NOT "FrameSaverVideoFilterImpl.hpp" STREQUAL "")
  if (TARGET kmskms_frame_saver_video_filterimpl)
    set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_GENERATED_INCLUDE_DIR "${KMSKMS_FRAME_SAVER_VIDEO_FILTER_SOURCE_DIR_PREFIX}/src/server/implementation/objects")
  else ()
    find_path(KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_GENERATED_INCLUDE_DIR
      NAMES
        FrameSaverVideoFilterImpl.hpp
      PATH_SUFFIXES
        src/server/implementation/objects
        kurento/modules/kms_frame_saver_video_filter
    )
  endif ()

  list (APPEND KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_GENERATED_INCLUDE_DIR})
  list (APPEND REQUIRED_VARS KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_GENERATED_INCLUDE_DIR)
endif()

if (NOT "" STREQUAL "")
  if (TARGET kmskms_frame_saver_video_filterimpl)
    set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_EXTRA_INCLUDE_DIR "${KMSKMS_FRAME_SAVER_VIDEO_FILTER_SOURCE_DIR_PREFIX}/")
  else ()
    find_path(KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_EXTRA_INCLUDE_DIR
      NAMES
        
      PATH_SUFFIXES
        
        kurento/modules/kms_frame_saver_video_filter
    )
  endif ()

  list (APPEND KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_EXTRA_INCLUDE_DIR})
  list (APPEND REQUIRED_VARS KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_EXTRA_INCLUDE_DIR)
endif ()

if (NOT "" STREQUAL "")
  if (TARGET kmskms_frame_saver_video_filterinterface)
    set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_EXTRA_INCLUDE_DIR "${KMSKMS_FRAME_SAVER_VIDEO_FILTER_SOURCE_DIR_PREFIX}/")
  else ()
    find_path(KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_EXTRA_INCLUDE_DIR
      NAMES
        
      PATH_SUFFIXES
        
        kurento/modules/kms_frame_saver_video_filter
    )
  endif()

  list (APPEND KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_EXTRA_INCLUDE_DIR})
  list (APPEND REQUIRED_VARS KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_EXTRA_INCLUDE_DIR)
endif ()

if (TARGET kmskms_frame_saver_video_filterimpl)
  set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARY kmskms_frame_saver_video_filterimpl)
else ()
  find_library (KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARY
    NAMES
      kmskms_frame_saver_video_filterimpl
    PATH_SUFFIXES
      ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_BINARY_DIR_PREFIX}/src/server
  )
endif()

set (REQUIRED_LIBS "")
foreach (LIB ${REQUIRED_LIBS})
  string(FIND ${LIB} " " POS)

  if (${POS} GREATER 0)
    string(REPLACE " " ";" REQUIRED_LIB_LIST ${LIB})
    include (CMakeParseArguments)
    cmake_parse_arguments("PARAM" "" "LIBNAME" "" ${REQUIRED_LIB_LIST})

    if (DEFINED PARAM_LIBNAME)
      generic_find (${REQUIRED_LIB_LIST} REQUIRED)
      set (LIB_NAME ${PARAM_LIBNAME})
    else()
      string (SUBSTRING ${LIB} 0 ${POS} LIB_NAME)
      string (SUBSTRING ${LIB} ${POS} -1 LIB_VERSION)
      string (STRIP ${LIB_NAME} LIB_NAME)
      string (STRIP ${LIB_VERSION} LIB_VERSION)
      generic_find (LIBNAME ${LIB_NAME} REQUIRED VERSION "${LIB_VERSION}")
    endif()
  else ()
    string (STRIP ${LIB} LIB_NAME)
    generic_find (LIBNAME ${LIB_NAME} REQUIRED)
  endif ()
  list (APPEND REQUIRED_LIBRARIES ${${LIB_NAME}_LIBRARIES})
  list (APPEND KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS ${${LIB_NAME}_INCLUDE_DIRS})

endforeach()

set(KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS
  ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS}
  CACHE INTERNAL "Include directories for KMSKMS_FRAME_SAVER_VIDEO_FILTER library" FORCE
)

set (KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARIES
  ${KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARY}
  ${KMSCORE_LIBRARIES}
  ${KMSELEMENTS_LIBRARIES}
  ${KMSFILTERS_LIBRARIES}
  ${REQUIRED_LIBRARIES}
  CACHE INTERNAL "Libraries for KMSKMS_FRAME_SAVER_VIDEO_FILTER" FORCE
)

include (FindPackageHandleStandardArgs)

find_package_handle_standard_args(KMSKMS_FRAME_SAVER_VIDEO_FILTER
  FOUND_VAR
    KMSKMS_FRAME_SAVER_VIDEO_FILTER_FOUND
  REQUIRED_VARS
    ${REQUIRED_VARS}
  VERSION_VAR
    KMSKMS_FRAME_SAVER_VIDEO_FILTER_VERSION
)

mark_as_advanced(
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_FOUND
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_VERSION
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_INCLUDE_DIR
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_INTERNAL_INCLUDE_DIR
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_GENERATED_INCLUDE_DIR
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_IMPLEMENTATION_EXTRA_INCLUDE_DIR
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_INTERFACE_EXTRA_INCLUDE_DIR
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_INCLUDE_DIRS
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARY
  KMSKMS_FRAME_SAVER_VIDEO_FILTER_LIBRARIES
)
